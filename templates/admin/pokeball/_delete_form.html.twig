<form method="post" action="{{ path('admin_pokeball_delete', {'id': poke_ball.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ poke_ball.id) }}">
    <button class="btn">Delete</button>
</form>
